// getTracksFromPlaylist = async function(playlistId){
//     const tracks = await cli.playlists.getTracks(playlistId);
//     var track = [];
//     for(i=0, x=1; i < tracks.length; i++,x++){
//             var gotTrack = {
//             id: x,
//             name: tracks[i].track.name,
//             artist: tracks[i].track.artists[0].name,
//             ytQuery: tracks[i].track.name + " - " + tracks[i].track.artists[0].name
//         };
//         track.push(gotTrack);
//     };
//     return track;
// };

// // const data = getTracksFromPlaylist("1JXmQxcfIdcKnU7gNw9tbP");